Read the uploaded file: "learnova project.docx" and my full project folder structure carefully BEFORE doing anything.

Your role: Senior full-stack engineer building a production-ready AI SaaS (student learning platform).

---

## STRICT RULES

- NO explanations unless absolutely necessary (max 1–2 lines if required)
- NO redesign suggestions (login + onboarding UI already final)
- DO NOT output everything at once
- Work STEP-BY-STEP only
- After each step, STOP and wait for my confirmation
- Every step must be COMPLETE, TESTABLE, and ERROR-FREE
- Review your own code before giving it
- Use tokens efficiently

---

## OUTPUT FORMAT (MANDATORY)

- Always give code only
- Include full file path like:
  /components/chatbot/index.jsx
  /server/routes/auth.js
- No extra text outside code unless critical

---

## 🎯 OBJECTIVE

Goal: Make Learnova FULLY WORKING for demo (1 day)

Focus ONLY on functionality:
- Replace mock data with real backend data
- Connect frontend ↔ backend
- Make chatbot working (Gemini)
- Make auth working (Google only)
- Make onboarding functional (file upload + selections)
- Make dashboard fully usable

DO NOT improve design  
You may REMOVE unnecessary frontend complexity

---

## 🔥 EXECUTION PRIORITY (VERY IMPORTANT)

Follow this exact order:

### 1. LOGIN (auth)
- Google login only
- No UI changes
- Replace mock logic → real auth flow

### 2. ONBOARDING
- File upload (PDF/text)
- User selections working
- Replace mock state → backend-connected

### 3. DASHBOARD (MAIN FOCUS)
- Clean unnecessary complexity
- Fix broken flows
- Connect:
  - chat
  - courses
  - analytics (basic)
- Ensure usable for demo

---

## 🧩 STEP PLAN

### STEP 1 — FRONTEND AUDIT (FOCUSED)

ONLY check:
- /components/auth
- /components/onboarding
- /components/dashboard

Ignore homepage completely

Check:
- Mock/fake data usage
- Broken state/props
- Missing API connections
- Over-complex components (mark for removal)

OUTPUT:
1. List of issues (short)
2. Fixed code (only necessary files)

STOP.

---

### STEP 2 — FRONTEND API PREP

- Remove ALL mock data
- Create API-ready structure:
  - services/api.js (or similar)
  - clean fetch/axios calls
- Fix state handling

OUTPUT:
- Updated files only

STOP.

---

### STEP 3 — BACKEND SETUP

Before code:
- Tell ONLY what to install (express, cors, dotenv, etc.)

Then:
- Folder structure
- Minimal server setup

STOP.

---

### STEP 4 — GOOGLE AUTH

- Implement Google OAuth only
- No manual login/signup
- Connect frontend + backend

STOP.

---

### STEP 5 — GEMINI CHATBOT

- Use @google/generative-ai (already installed)
- Backend route for chat
- Connect dashboard chat UI → backend

STOP.

---

### STEP 6 — FILE UPLOAD

- Backend upload endpoint
- Accept PDF/text
- Connect onboarding UI

STOP.

---

### STEP 7 — FINAL CHECK

Ensure:
- Login works
- Onboarding works
- Chat works
- Dashboard usable
- No mock data

Fix only critical issues

---

## ⚠️ IMPORTANT

- Use existing stack from project (Next.js + current libs)
- DO NOT add unnecessary frameworks
- Keep backend minimal (demo-ready only)
- Avoid overengineering
- Dashboard is the MAIN presentation area → prioritize stability

---

## 📁 PROJECT STRUCTURE

LEARNOVA/
├── .next/
├── app/
│   ├── (auth)/
│   ├── (dashboard)/
│   ├── (onboarding)/
│   ├── api/
│   ├── loader/
│   ├── favicon.ico
│   ├── globals.css
│   ├── layout.tsx
│   ├── page.jsx
│
├── components/
│   ├── animation/
│   ├── auth/
│   ├── charts/
│   ├── common/
│   ├── dashboard/
│   │   ├── admin/
│   │   ├── analytics/
│   │   ├── chat/
│   │   ├── concept/
│   │   ├── courses/
│   │   ├── layout/
│   │   ├── overview/
│   │   ├── plan/
│   │   ├── quiz/
│   │   ├── revision/
│   │   ├── settings/
│
│   ├── onboarding/
│   │   ├── OnboardingCard.jsx
│   │   ├── OnboardingHeader.jsx
│   │   ├── OnboardingSidebar.jsx
│   │   ├── StepComplete.jsx
│   │   ├── StepConfidence.jsx
│   │   ├── StepCourse.jsx
│   │   ├── StepFeatures.jsx
│   │   ├── StepFocus.jsx
│   │   ├── StepFooter.jsx
│   │   ├── StepLabel.jsx
│   │   ├── StepProcessing.jsx
│   │   ├── StepTimeline.jsx
│   │   ├── StepWelcome.jsx
│
├── lib/
│   ├── gsap.ts
│   ├── nivo.ts
│   ├── utils.ts

---

Start with STEP 1 only.

## BACKEND STACK DECISION (IMPORTANT)

Use:
- Node.js + Express
- MongoDB (Mongoose)

Do NOT use PostgreSQL (skip for speed)

Skip:
- Tesseract OCR (not required for demo)
- Advanced analytics
- React Flow features

Focus only on:
- Auth (Google)
- Chat (Gemini)
- File upload (basic text/PDF only, no OCR)
- User data storage

- If any step requires missing files, ask before proceeding